general: {
    #configs_folder = /usr/local/etc/janus/streams
}

rtp-sample: {
    type = "rtp"
    id = 1
    description = "VP8 video-only from external RTP source"
    audio = false
    video = true

    # VIDEO (wajib lengkap)
    videoport = 5004
    videortcpport = 5005
    videopt = 100
    videocodec = "vp8"
    # kadang membantu:
    videoiface = "0.0.0.0"

    # workaround untuk versi tertentu (boleh dibiarkan):
    audioport = 9999

    secret = "adminpwd"
}
